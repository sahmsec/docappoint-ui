export const metadata = {
  title: 'All Appointments',
  description: 'Browse all available doctors and book your appointment today. Search by doctor name and filter by specialty.',
};

export default function AppointmentsLayout({ children }) {
  return children;
}
