export const metadata = {
  title: 'Login',
  description: 'Login to your DocAppoint account to book appointments and manage your healthcare.',
};

export default function LoginLayout({ children }) {
  return children;
}
