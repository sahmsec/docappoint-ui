export const metadata = {
  title: 'Register',
  description: 'Create your DocAppoint account to start booking doctor appointments online.',
};

export default function RegisterLayout({ children }) {
  return children;
}
